# Base URL of the future SPORTX Node.js API (server/). Leave empty while using mock services.
VITE_API_URL=
# Set to "false" once the backend is live to route services through the HTTP client.
VITE_USE_MOCK_API=true
# Public site origin, used for canonical URLs.
VITE_SITE_URL=http://localhost:5173

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node dist/scripts/migrate.js && node dist/src/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Admin public assets

Place the official SPORTX logo here as `public/logo.png` (same file as `customer/public/logo.png`).
Then set `LOGO_SRC` in `src/constants/brand.ts` to `'/logo.png'`. Until a logo file exists the
admin renders the SPORTX typographic wordmark so no image is ever broken.

# Admin — Railway production (public, baked in at build time).
# Paste into the admin service Variables tab. Do NOT wrap values in quotes.
VITE_API_URL=https://vigilant-respect-production-14a3.up.railway.app
